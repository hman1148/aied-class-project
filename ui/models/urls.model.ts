export enum Page {
  GetStocks = '/all',
  BuyStocks = '/buy',
  Question = '/question',
  Answer = '/answer',
  History = '/history',
  ResetHistory = '/reset-history',
}

export enum Apis {
  Stocks = '/stocks',
  Tutor = '/tutor',
}
