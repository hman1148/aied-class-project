export enum Page {
  GetStocks = '/all',
  BuyStocks = '/buy',
  Question = '/question',
  RandomQuestion = '/question/random-scenario',
  Answer = '/answer',
  History = '/history',
  ResetHistory = '/reset-history',
}

export enum Apis {
  Stocks = '/stocks',
  Tutor = '/tutor',
}
